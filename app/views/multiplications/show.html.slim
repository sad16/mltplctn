.multiplication
  .none#multiplication_id = @multiplication.id
  - if @multiplication.sum.nil?
    .multiplication__expression
      #multiplicand multiplicand
      span
        | *
      #multiplier multiplier
      span 
        | =
      #result result
    .multiplication__sum
      #sum sum
  - else
    .multiplication__sum
      #sum = @multiplication.sum